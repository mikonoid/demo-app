apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-app
  template:
    metadata:
      labels:
        app: demo-app
    spec:
      containers:
      - image: index.docker.io/mk51/demo-app:c76574f0994a9ec5765ef27e37ecea1767412622
        name: demo-app
        ports:
        - containerPort: 8000
