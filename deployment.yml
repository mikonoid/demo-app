apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-app
  template:
    metadata:
      labels:
        app: demo-app
    spec:
      containers:
      - image: index.docker.io/mk51/demo-app:02c6a00252d0ca2cae0be546093877bdd67e3c8c
        name: demo-app
        ports:
        - containerPort: 8000
